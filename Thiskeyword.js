// this keyword refers to the object that is currently executing the code. It can be used to access properties and methods of the current object. In the example above, this.name and this.age refer to the name and age properties of the person object. The greet method uses this to access these properties and log a message to the console.
// 1) Implicit Binding: When a function is called as a method of an object, this refers to the object that the method is called on. For example, in the greet method above, this refers to the person object because greet is called as a method of person.
// 2) Explicit Binding: When a function is called with the call() or apply() methods, this can be explicitly set to refer to a specific object. For example, if we have another object called anotherPerson and we want to use the greet method of person on anotherPerson, we can do so by calling person.greet.call(anotherPerson). In this case, this will refer to anotherPerson inside the greet method.
// 3) New Binding: When a function is called with the new keyword, this refers to the newly created object. For example, if we have a constructor function called Person and we create a new instance of it using new Person(), this will refer to the new instance inside the constructor function.
// 4) Default Binding: If none of the above rules apply, this will refer to the global object (window in browsers, global in Node.js). However, in strict mode, this will be undefined.

